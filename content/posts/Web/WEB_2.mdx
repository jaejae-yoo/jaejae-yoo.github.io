---
title:  DOM & Event 정리 1
date: 2021-03-11
tags:
  - WEB
---

| Number | Title                                    | Year |
| ------ | ---------------------------------------- | ---: |
| 1      | WEB | 2021.03.12 |

> ## DOM

##### DOM & EVENT 자주 헷갈리는 내용 정리
<br></br>

### Document.createElement()
HTML 문서에서, Document.createElement() 메서드는 지정한 tagName의 HTML 요소를 만들어 반환한다. 


### Element.setAttribute()
.setAttribute()는 선택한 요소(element)의 속성(attribute) 값을 결정한다.
element.setAttribute( 'attributename', 'attributevalue' )
attributename에는 속성 이름을, attributevalue에는 속성값을 넣는다.


### Element.classList
Element.classList는 엘리먼트의 클래스 속성의 컬렉션인 활성 DOMTokenList를 반환하는 읽기 전용 프로퍼티이다.

Element.classList.add( String [, String [, ...]] )
  지정한 클래스 값을 추가한다. 만약 추가하려는 클래스가 엘리먼트의 class 속성에 이미 존재한다면 무시한다.



> ## Event

### EventTarget 
EventTarget은 이벤트를 받을 수 있으며, 이벤트에 대한 수신기(listener)를 가질 수 있는 객체가 구현하는 DOM 인터페이스이다.

    - EventTarget.addEventListener()
    EventTarget에 특정 이벤트 유형의 이벤트 처리기를 등록합니다.
    - EventTarget.removeEventListener()
    EventTarget에서 주어진 이벤트 수신기를 제거합니다.
    - EventTarget.dispatchEvent()
    EventTarget에 이벤트를 디스패치(일을 처리한다) 합니다.


### Bubbling

한 요소에 이벤트가 발생하면, 이 요소에 할당된 핸들러가 동작하고, 이어서 부모 요소의 핸들러가 동작한다.
가장 최상단의 조상 요소를 만날 때까지 이 과정이 반복되면서 요소 각각에 할당된 핸들러가 동작한다.

    event.target
    event.target은 이벤트버블링의 가장 마지막에 위치한 최하위의 요소를 반환한다.
    즉 클릭된 요소를 기준으로 사용하는 경우 event.target을 사용한다.
    
    event.currentTarget
    event.currentTarget은 이벤트가 부착된 부모의 위치를 반환한다.


#### Bubbling을 막을 수 있는 방법

1. Event.stopPropagation() (이벤트 캡쳐링과 버블링에 있어 현재 이벤트 이후의 전파를 막는다.)
   Event.stopImmediatePropagation() (같은 이벤트에서 다른 리스너들이 불려지는 것을 막는다.)
  
  ➜규모있는 프로젝트에서 위의 방법들을 사용는 것은 굉장히 위험하므로 지양해야 한다.

2. event.target == event.currentTarget이 같을 때 해당 이벤트를 처리하면 됨 


  ### 참고문헌
- [MDN](https://developer.mozilla.org/ko/)